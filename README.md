# ClariPix

## Cách chạy local server backend (swagger)
`cd server`

`npm run dev`

Swagger UI running at: `http://localhost:3000/api-docs`

docker-docker run -p 3000:3000 -e NEXT_PUBLIC_API_URL=http://localhost:3618 nextjs-app